[project]
name = "ustat-explorer-backend"
version = "0.1.0"
description = "https://docs.astral.sh/uv/"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "understatapi==0.7.1",
    "fastapi[standard]",
    "ipykernel>=7.3.0",
    "pandas==3.0.3",
]
